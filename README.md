# local-monitoring

6. Monitoring Stack with Prometheus + Grafana
Objective: Deploy and configure local monitoring for a containerized app.
• Tasks:
o Set up a sample app with a /metrics endpoint (use Prometheus format)
o Deploy Prometheus + Grafana using Docker Compose
o Visualize custom metrics in Grafana
o Create a dashboard and export it

<!-- To deploy the docker compose -->
docker-compose up -d --build